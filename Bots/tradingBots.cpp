botCommunicationWay::botCommunicationWay(const tradingBot & bot, const std::string & data, const std::string & evn, const std::string & ordr):
     adrBot(&bot), dataPath(data), eventPath(evn), orderPath(order){
     
     }
